services:
  bot:
    image: ghcr.io/denizz259/networking-bot:latest
    container_name: network-bot
    env_file:
      - .env.prod
    environment:
      DATABASE_URL: sqlite:////data/db.sqlite
      LOG_LEVEL: INFO
    volumes:
      - bot_data:/data
    restart: always

volumes:
  bot_data:
